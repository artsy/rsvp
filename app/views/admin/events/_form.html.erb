<%= form_for(event, url: admin_events_path) do |f| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :presented_by %>
    <%= f.text_field :presented_by %>
  </div>

  <div class="field">
    <%= f.label :guests_allowed %>
    <%= f.number_field :guests_allowed %>
  </div>

  <div class="field">
    <%= f.label :capacity %>
    <%= f.number_field :capacity %>
  </div>

  <div class="field">
    <%= f.label :lock_fields %>
    <%= f.check_box :lock_fields %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
