machine:
  ruby:
    version: 2.3.1
deployment:
  production:
    branch: release
    owner: artsy
    commands:
      - git push git@heroku.com:artsy-rsvp.git $CIRCLE_SHA1:refs/heads/master
      - heroku run --exit-code rake db:migrate --app artsy-rsvp
      - heroku restart --app artsy-rsvp
